package com.aura.reporting.domain.model;

/**
 * Aggregate Root for the Reporting context.
 *
 * <p>Represents a structured incident report with a lifecycle:
 * Draft → Edited → Approved → Exported.
 *
 * TODO: implement after hackathon
 */
public class IncidentReport {
    // TODO: implement after hackathon
    // Fields: id, incidentId, userId, status, sections, approvedBy, exportedAt
    // Methods: createDraft(), edit(ReportSection), approve(), export(ExportFormat)
}
