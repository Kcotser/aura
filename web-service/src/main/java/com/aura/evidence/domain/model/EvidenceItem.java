package com.aura.evidence.domain.model;

/**
 * Aggregate Root for the Evidence context.
 *
 * <p>Represents a single piece of multimedia evidence (audio, video, photo)
 * associated with an incident. Only metadata and a storage reference are persisted.
 *
 * TODO: implement after hackathon
 */
public class EvidenceItem {
    // TODO: implement after hackathon
    // Fields: id, incidentId, userId, mediaType, storageReference, capturedAt, metadata
    // Methods: attach(), validate(), markProcessed()
}
